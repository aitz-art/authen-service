const mongoose = require('mongoose');
const Schema = mongoose.Schema;
let <var> = new Schema({
    <schema>: { type: <Type>}
},{
    versionKey: false
}
);
module.exports = mongoose.model('<var>', <var> ,"<database_name>");
